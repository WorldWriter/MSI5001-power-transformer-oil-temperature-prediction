# Requirements for ResNet Model Training
# Windows CUDA Environment
#
# Installation Instructions:
# 1. Install PyTorch with CUDA support:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#    (Replace cu118 with your CUDA version: cu117, cu118, cu121, etc.)
#
# 2. Install other requirements:
#    pip install -r requirements-resnet.txt

# Core dependencies (should already be installed for baseline models)
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
matplotlib>=3.4.0
seaborn>=0.11.0
joblib>=1.0.0
scipy>=1.7.0

# PyTorch - Install separately with CUDA support (see instructions above)
# torch>=2.0.0
# torchvision>=0.15.0

# Jupyter notebook support
jupyter>=1.0.0
notebook>=6.4.0
ipykernel>=6.0.0
ipywidgets>=7.6.0

# Progress bars and utilities
tqdm>=4.62.0

# Optional: For better plots
plotly>=5.0.0

# Optional: For model analysis
torchinfo>=1.7.0
torchsummary>=1.5.0
